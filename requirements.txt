# WHOAI FastAPI runtime (gateway / ingestion plane) dependencies.
# Only the packages the backend actually imports — keep this list in sync with
# the imports under runtime/ and database/.

# Web framework + server
fastapi==0.137.1
uvicorn[standard]==0.49.0

# Database (async SQLAlchemy + Postgres driver; aiosqlite for local dev/tests)
SQLAlchemy==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1
greenlet==3.5.1

# LLM provider SDKs (BYOK gateway)
openai==2.41.1
anthropic==0.109.2
google-generativeai==0.8.6

# Auth, crypto, config, validation
PyJWT==2.13.0
cryptography==49.0.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- Dev / test only (safe to install in CI; not required at runtime) ---
pytest==9.1.0
pytest-asyncio==1.4.0
