services:
  - type: web
    name: whoai-api
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn runtime.main:app --host 0.0.0.0 --port $PORT

    healthCheckPath: /health